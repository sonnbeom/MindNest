# Product.md — 마음 상담소

> 프로젝트의 목적, 플로우, 기능 범위를 정의하는 문서입니다.
> 코드 작성 전 이 문서를 읽고 제품의 의도를 이해하세요.

---

## 1. 제품 정의

**무엇인가**
CBT(인지행동치료) 프로토콜 기반 셀프 심리 상담 웹 앱.
인지 왜곡을 분석하고, 치료 기법을 통해 사용자 스스로 인지를 재구성하도록 돕는다.

**무엇이 아닌가**
- 단순 AI 챗봇
- 전문 심리 치료 서비스
- 진단 또는 처방 도구

**핵심 차별점**
LLM을 단순 호출하지 않고, 구조화된 세션 스테이트 머신으로 실제 상담 흐름을 구현한다.

---

## 2. 세션 플로우

**5단계 고정 순서**

```
1. INTAKE
   사용자가 현재 감정 강도(SUD 0~10)를 입력하고 상담 내용을 자유 서술

2. DISTORTION_ANALYSIS
   LLM이 작성 내용에서 인지 왜곡 패턴을 분석
   → 왜곡 유형, 근거 구절, 설명을 JSON으로 반환
   → 사용자에게 분석 결과 표시 및 확인

3. REFRAME
   부정적 감정 이면의 긍정적 가치를 탐색
   → 사전 정의된 긍정 가치 카드 제시 (LLM 호출 없음)
   → 사용자가 선택하거나 직접 입력
   → 힌트는 사전 데이터셋 룩업 (LLM 호출 없음)

4. THERAPY
   분석된 왜곡 유형에 따라 적합한 치료 기법을 제공
   → 기법별 전용 UI 컴포넌트로 렌더링
   → 현재 MVP: CBT(소크라테스식 대화) 우선 구현

5. CLOSE
   세션 후 SUD 재측정 및 감정 변화 기록
   → 세션 요약 카드 표시
   → 감정 이력 차트 업데이트
```

---

## 3. 치료 기법 및 UI 컴포넌트

### 컴포넌트 구조 원칙

SessionHeader, SessionFooter는 모든 기법이 공유한다.
TherapyContent 안에서 기법 타입에 따라 컴포넌트를 교체한다.

```
TherapySession (공통 래퍼)
├── SessionHeader       # 단계 표시, 진행률, 현재 기법명
├── TherapyContent      # 기법 타입을 props로 받아 컴포넌트 교체
│     ├── ChatTherapy        # CBT 소크라테스식 대화
│     ├── ChecklistTherapy   # 행동 활성화 (BA)
│     ├── StepperTherapy     # 노출 치료
│     ├── TimerTherapy       # 마음챙김 (MBSR)
│     ├── FormTherapy        # 사고 기록지
│     └── CompareTherapy     # 행동 실험
└── SessionFooter       # SUD 슬라이더, 다음 단계 버튼
```

### 기법별 정의

| 기법 | 컴포넌트 | UI 패턴 | MVP 포함 | LLM |
|------|---------|--------|---------|-----|
| CBT 소크라테스식 대화 | ChatTherapy | 채팅형 | ✅ | ✅ 턴당 1회 |
| 행동 활성화 (BA) | ChecklistTherapy | 캘린더/체크리스트 | ❌ | ❌ |
| 노출 치료 | StepperTherapy | 단계별 스텝퍼 | ❌ | ❌ |
| 마음챙김 (MBSR) | TimerTherapy | 타이머/가이드 | ❌ | ❌ |
| 사고 기록지 | FormTherapy | 폼/테이블 | ❌ | ❌ |
| 행동 실험 | CompareTherapy | 예측→결과 비교 카드 | ❌ | ❌ |

### CBT — ChatTherapy 소크라테스식 5턴 구조

| Turn | 목표 | 방향 |
|------|------|------|
| 0 | 자동적 사고 탐색 | "그 순간 가장 먼저 떠오른 생각은?" |
| 1 | 증거 검토 | "그 생각을 지지하는 근거와 반대 근거는?" |
| 2 | 대안적 관점 | "친한 친구라면 이 상황에 뭐라 할까?" |
| 3 | 인지 재구성 | "처음 생각을 다르게 표현한다면?" |
| 4 | 행동 계획 | "이번 주 작게 시도해볼 수 있는 것은?" |

---

## 4. 인지 왜곡 분류 기준

Beck의 10가지 인지 왜곡. LLM 분석 및 치료 기법 매칭의 기준이 된다.

```
흑백논리      / 파국화       / 독심술
감정적 추론   / 과잉일반화   / 개인화
선택적 추상화 / 축소·과장   / 당위적 사고 / 낙인찍기
```

---

## 5. 감정 지수 (SUD)

- 척도: 0~10 정수
- 0 = 완전히 평온 / 10 = 극도로 힘듦
- 측정: 세션 시작(intakeSUD) + 세션 종료(closeSUD) 두 번
- 이력: 세션 간 누적 저장, 시계열 차트로 시각화

---

## 6. MVP 범위

**포함**
- 세션 5단계 플로우
- 인지 왜곡 분석 (LLM)
- 감정 이면 탐색 (사전 데이터)
- CBT 소크라테스식 5턴 대화 (LLM)
- SUD 전후 기록 및 감정 이력 차트
- TherapySession 공통 래퍼 (향후 기법 추가를 위한 구조)

**미포함 (Post-MVP)**
- CBT 외 치료 기법 컴포넌트
- 사용자 계정 및 서버 저장
- RAG 도메인 지식 주입
- Fine-tuning
- 멀티 에이전트 파이프라인
- 위기 감지 및 전문가 연결
- 개인정보 처리방침 UI

---

## 7. 면책 고지

앱 내 반드시 표시.

```
본 앱은 전문 심리 치료를 대체하지 않습니다.
심각한 심리적 어려움이 있다면 전문가 상담을 받으세요.
```

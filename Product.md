# Product.md — 마음 상담소

> 프로젝트의 목적, 플로우, 기능 범위를 정의하는 문서입니다.
> 코드 작성 전 이 문서를 읽고 제품의 의도를 이해하세요.

---

## 1. 제품 정의

**무엇인가**
CBT(인지행동치료) 프로토콜 기반 셀프 심리 상담 웹 앱.
인지 왜곡을 분석하고, 치료 기법을 통해 사용자 스스로 인지를 재구성하도록 돕는다.

**무엇이 아닌가**
- 단순 AI 챗봇
- 전문 심리 치료 서비스
- 진단 또는 처방 도구

**핵심 차별점**
LLM을 단순 호출하지 않고, 구조화된 상담 스테이트 머신으로 실제 상담 흐름을 구현한다.

---

## 2. 상담 플로우

**5단계 고정 순서**

```
1. INTAKE
   사용자가 현재 감정 강도(SUD 0~10)를 입력하고 상담 내용을 자유 서술

2. DISTORTION_ANALYSIS
   LLM이 작성 내용에서 인지 왜곡 패턴을 분석
   → 왜곡 유형, 근거 구절, 설명을 JSON으로 반환
   → 사용자에게 분석 결과 표시 및 확인
   → 각 왜곡에 대한 힌트(생각해볼 질문)를 사전 데이터셋 룩업으로 제시 (LLM 호출 없음)

3. REFRAME — 긍정적 재구성
   부정적 감정 이면의 긍정적 가치를 탐색
   → 각 인지 왜곡에 대해 사용자가 긍정적 재구성 텍스트를 직접 입력 (LLM 호출 없음)
   → 예시 텍스트를 토글로 참고 가능 (사전 정의 데이터)
   → 최소 1개 이상 입력 시 다음 단계로 진행 가능

4. CBT_DIALOGUE — 생각 균형 잡기
   각 인지 왜곡에 대해 증거를 조사하고 균형잡힌 생각을 도출
   → 왜곡별 3개 영역 입력:
     ① 참이라는 증거 (이 생각이 사실일 수도 있다는 근거)
     ② 거짓이라는 증거 (이 생각이 사실이 아닐 수도 있다는 근거)
     ③ 균형잡힌 생각 (더 균형잡힌 대안적 생각)
   → LLM 호출 없음 (정적 폼)
   → 균형잡힌 생각 1개 이상 입력 시 다음 단계로 진행 가능

5. CLOSE — 오늘의 마무리
   상담 후 SUD 재측정 및 감정 변화 기록
   → 상담 요약 카드 표시 (시작/종료 SUD, 발견한 왜곡, 긍정적 재구성 내용)
   → 감정 이력 차트 업데이트
```

---

## 3. UI 컴포넌트 구조

```
TherapySession (공통 래퍼)
├── SessionHeader     # 단계 번호, 단계명, 진행률 바, 면책 고지
├── (단계별 컴포넌트)
│     ├── IntakeStage        # INTAKE: SUD 슬라이더 + 자유 서술 텍스트
│     ├── DistortionStage    # DISTORTION_ANALYSIS: 왜곡 카드 + 힌트
│     ├── ReframeStage       # REFRAME: 왜곡↔긍정 재구성 2열 입력 테이블
│     ├── EvidenceStage      # CBT_DIALOGUE: 참/거짓/균형잡힌 생각 3개 영역 폼
│     └── CloseStage         # CLOSE: SUD 전후 비교 + 요약 카드 + 마무리 슬라이더
└── SessionFooter     # 다음 단계 버튼, 진행 불가 안내 메시지
```

**단계별 레이블 (SessionHeader 표시)**

| 단계 | 번호 | 레이블 |
|------|------|------|
| INTAKE | 1 | 감정 지수 |
| DISTORTION_ANALYSIS | 2 | 인지 왜곡 분석 |
| REFRAME | 3 | 긍정적 재구성 |
| CBT_DIALOGUE | 4 | 생각 균형 잡기 |
| CLOSE | 5 | 오늘의 마무리 |

---

## 4. 인지 왜곡 분류 기준

Beck의 10가지 인지 왜곡. LLM 분석의 기준이 된다.

```
흑백논리      / 파국화       / 독심술
감정적 추론   / 과잉일반화   / 개인화
선택적 추상화 / 축소·과장   / 당위적 사고 / 낙인찍기
```

---

## 5. 감정 지수 (SUD)

- 척도: 0~10 정수
- 0 = 완전히 평온 / 10 = 극도로 힘듦
- 측정: 상담 시작(intakeSUD) + 상담 종료(closeSUD) 두 번
- 이력: 상담 간 누적 저장, 시계열 차트로 시각화

---

## 6. MVP 범위

**포함**
- 상담 5단계 플로우
- 인지 왜곡 분석 (LLM, DISTORTION_ANALYSIS 단계)
- 긍정적 재구성 직접 입력 (REFRAME 단계, LLM 없음)
- 증거 조사를 통한 생각 균형 잡기 (CBT_DIALOGUE 단계, LLM 없음)
- SUD 전후 기록 및 감정 이력 차트

**미포함 (Post-MVP)**
- CBT 소크라테스식 5턴 LLM 대화 (ChatTherapy)
- 행동 활성화, 노출 치료, 마음챙김, 사고 기록지, 행동 실험 컴포넌트
- 사용자 계정 및 서버 저장
- RAG 도메인 지식 주입
- Fine-tuning
- 멀티 에이전트 파이프라인
- 위기 감지 및 전문가 연결
- 개인정보 처리방침 UI

---

## 7. 면책 고지

앱 내 반드시 표시.

```
본 앱은 전문 심리 치료를 대체하지 않습니다.
심각한 심리적 어려움이 있다면 전문가 상담을 받으세요.
```
